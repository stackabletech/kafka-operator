apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: opa-operator-kafka-broker-clusterrole
rules:
  - apiGroups:
      - ""
    resources:
      - services
    verbs:
      - get
