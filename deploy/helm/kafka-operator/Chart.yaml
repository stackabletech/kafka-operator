---
apiVersion: v2
name: kafka-operator
version: "0.0.0-dev"
appVersion: "0.0.0-dev"
description: "Kubernetes operator for Apache Kafka. Deploy and run Kafka brokers with the Stackable Data Platform (SDP)."
home: https://github.com/stackabletech/kafka-operator
sources:
  - https://github.com/stackabletech/kafka-operator
keywords: ["apache-kafka", "kafka", "streaming", "messaging", "kubernetes", "operator", "big-data"]
maintainers:
  - name: Stackable
    url: https://www.stackable.tech
# Consumed by Artifact Hub, see https://artifacthub.io/docs/topics/annotations/helm/
annotations:
  # The chart package ships no LICENSE file, so Artifact Hub cannot detect this itself.
  artifacthub.io/license: OSL-3.0
  # A link named "support" is rendered highlighted by Artifact Hub. Keep it lowercase.
  artifacthub.io/links: |
    - name: Documentation
      url: https://docs.stackable.tech/home/stable/kafka/
    - name: support
      url: https://github.com/stackabletech/kafka-operator/issues
