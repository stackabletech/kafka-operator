//! Product-agnostic building blocks, staged here until they move to operator-rs. The module paths
//! mirror operator-rs, so moving code there only changes imports.

pub mod commons;
pub mod constants;
pub mod kvp;
